# Machine-Readable Contracts Index

Generated by `gendoc-gen-contracts` from gendoc documentation pipeline.
Source documents: docs/EDD.md, docs/API.md, docs/SCHEMA.md, docs/ARCH.md

## Artifact Catalog

| Artifact | Path | Purpose | Source |
|----------|------|---------|--------|
| OpenAPI 3.1 YAML | [docs/blueprint/contracts/openapi.yaml](blueprint/contracts/openapi.yaml) | API 合約，可匯入 Postman / Swagger UI / code generator（47 paths, 77 schemas） | docs/API.md |
| JSON Schema | [docs/blueprint/contracts/schemas/](blueprint/contracts/schemas/) | DTO 型別定義，供 validator / IDE / code generator 使用（7 schemas） | docs/SCHEMA.md |
| Pact Contracts | [docs/blueprint/contracts/pact/](blueprint/contracts/pact/) | Consumer-driven 合約測試規格（3 pairs, 39 interactions） | docs/API.md |
| Admin OpenAPI YAML | [docs/blueprint/contracts/admin/admin-api.yaml](blueprint/contracts/admin/admin-api.yaml) | Admin API 合約（/admin/api/* 所有 endpoint） | docs/API.md §6 |
| RBAC Seed | [docs/blueprint/contracts/admin/rbac-seed.json](blueprint/contracts/admin/rbac-seed.json) | Role/Permission 初始資料 | docs/ADMIN_IMPL.md |
| Admin Seed | [docs/blueprint/contracts/admin/admin-seed.json](blueprint/contracts/admin/admin-seed.json) | 本地開發初始 admin 帳號 | internal |
| Docker Compose | [docker-compose.yml](../docker-compose.yml) | 本地開發環境（api + postgres + redis + mailhog） | docs/EDD.md §4.5.9 |
| Helm Values | [docs/blueprint/infra/helm/values.yaml](blueprint/infra/helm/values.yaml) | Kubernetes 部署配置 | docs/EDD.md §4.5.9 |
| Network Policy | [docs/blueprint/infra/k8s/network-policy.yaml](blueprint/infra/k8s/network-policy.yaml) | 網路存取控制規則 | docs/EDD.md §4.5.9 |
| Seed Code | [docs/blueprint/scaffold/src/](blueprint/scaffold/src/) | TypeScript 骨架代碼（12 files）+ 測試骨架 | docs/EDD.md §4.5.2 |

## Usage Guide

### OpenAPI / Swagger UI
```bash
# Validate
npx @redocly/openapi-cli lint docs/blueprint/contracts/openapi.yaml

# Generate client SDK (TypeScript)
npx openapi-generator-cli generate \
  -i docs/blueprint/contracts/openapi.yaml \
  -g typescript-axios \
  -o src/generated/client

# Start Swagger UI
npx swagger-ui-serve docs/blueprint/contracts/openapi.yaml
```

### JSON Schema Validation
```bash
# Validate a payload against schema
npx ajv validate \
  -s docs/blueprint/contracts/schemas/Pet.schema.json \
  -d payload.json
```

### Pact Contract Testing
```bash
# Provider verification
npx pact-verifier \
  --pact-urls docs/blueprint/contracts/pact/*.json \
  --provider-base-url http://localhost:3000 \
  --provider-states-setup-url http://localhost:3000/_pact/setup
```

### Local Development
```bash
# Start all services
cp .env.example .env  # fill in your values
docker-compose up -d

# API available at http://localhost:3000
# Swagger UI: http://localhost:3000/docs
# MailHog UI: http://localhost:8025
```

### Scaffold
```bash
# Seed code is in docs/blueprint/scaffold/ — copy to begin implementation
cp -r docs/blueprint/scaffold/src/ src/
cp -r docs/blueprint/scaffold/tests/ tests/
```

## Coverage Report

| Step | Status | Details |
|------|--------|---------|
| OpenAPI 3.1 | ✅ | 47 paths (player + admin + health), 77 schemas |
| JSON Schema | ✅ | 7 core DTOs (Pet, ClaimCode, BattleRecord, LeaderboardEntry, FoodItem, ErrorResponse, PaginationMeta) |
| Pact Contracts | ✅ | 3 consumer-provider pairs, 39 total interactions (happy + error paths) |
| Admin Contracts | ✅ | admin-api.yaml + rbac-seed.json + admin-seed.json |
| IaC (Docker) | ✅ | docker-compose.yml + .env.example |
| IaC (K8s) | ✅ | helm/values.yaml + k8s/network-policy.yaml |
| Seed Code | ✅ | 12 TypeScript files (domain + application + infra + routes + tests) |

---
*Generated: 2026-05-05*
*Project: pixel-pet-arena*
